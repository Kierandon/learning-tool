<div class="max-w-4xl mx-auto">
  <div class="bg-white shadow rounded-lg p-6 mb-8">
    <div class="flex items-center justify-between mb-6">
      <h1 class="text-3xl font-bold text-gray-900">Available Standards</h1>
      <div class="flex items-center space-x-4">
        <% if current_user %>
          <div class="flex items-center px-3 py-2 rounded bg-yellow-50">
            <svg class="h-5 w-5 text-yellow-500 mr-2" fill="currentColor" viewBox="0 0 20 20">
              <path d="M10 18a8 8 0 100-16 8 8 0 000 16zM10 4a6 6 0 110 12 6 6 0 010-12z"/>
              <path fill-rule="evenodd" d="M10 6a4 4 0 100 8 4 4 0 000-8z" clip-rule="evenodd"/>
            </svg>
            <span class="font-medium text-yellow-800"><%= current_user.points %></span>
          </div>
        <% end %>
      </div>
    </div>

    <div class="bg-blue-50 border border-blue-200 rounded-lg p-4 mb-6">
      <h3 class="text-lg font-semibold text-blue-800 mb-2">Welcome to the Standards Library!</h3>
      <p class="text-blue-700 mb-2">
        Here you can explore all available standards. Each standard contains multiple courses designed to help you learn and understand the material.
      </p>
      <p class="text-blue-700">
        As you progress through courses, you'll earn coins, complete daily challenges, and unlock achievements.
      </p>
    </div>

    <div class="space-y-4">
      <% @standards.each do |standard| %>
        <%= render Standards::StandardCardComponent.new(standard: standard, user: current_user) %>
      <% end %>
    </div>
  </div>
</div> 
