<%= turbo_frame_tag "step_content", data: { turbo_action: "advance" } do %>
  <div data-controller="scroll">
    <%= render Progress::StepProgressBarComponent.new(course: @course, current_step: @step, user: current_user) %>
    <%= render Steps::StepComponent.new(step: step, course: course, just_answered: just_answered) %>
  </div>
<% end %>
