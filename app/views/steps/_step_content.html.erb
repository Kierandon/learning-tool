<%= turbo_frame_tag "step_content", data: { turbo_action: "advance" } do %>
  <%= render StepProgressBarComponent.new(course: @course, current_step: @step, user: current_user) %>
  <%= render StepComponent.new(step: step, course: course, just_answered: just_answered) %>
<% end %>
