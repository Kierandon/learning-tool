<nav class="flex items-center justify-between p-4 bg-white shadow">
  <div class="flex items-center">
    <div class="text-xl font-semibold px-3 py-2 border-r border-gray-300" data-testid="navbar-title">
      <%= link_to "Learning Tool", root_path, class: "hover:text-blue-600 transition duration-150" %>
    </div>
    <% if current_user %>
      <div class="flex items-center space-x-4 ml-4">
        <%= link_to "Standards", standards_path, class: "px-3 py-2 rounded hover:bg-gray-100 transition duration-150 flex items-center" %>
        <%= link_to "Daily Quests", profile_path, class: "px-3 py-2 rounded hover:bg-gray-100 transition duration-150 flex items-center" %>
        <div class="flex items-center px-3 py-2 rounded bg-yellow-50">
          <svg class="h-5 w-5 text-yellow-500 mr-2" fill="currentColor" viewBox="0 0 20 20">
            <path d="M10 18a8 8 0 100-16 8 8 0 000 16zM10 4a6 6 0 110 12 6 6 0 010-12z"/>
            <path fill-rule="evenodd" d="M10 6a4 4 0 100 8 4 4 0 000-8z" clip-rule="evenodd"/>
          </svg>
          <span class="font-medium text-yellow-800"><%= current_user.points %></span>
        </div>
      </div>
    <% end %>
  </div>

  <div class="flex items-center space-x-4">
    <% if current_user %>
      <div class="flex items-center space-x-3">
        <%= link_to "My Profile", profile_path, class: "px-3 py-2 rounded bg-gray-100 hover:bg-gray-200 transition duration-150 flex items-center" if defined?(profile_path) %>
        <span class="h-8 w-px bg-gray-300 mx-1"></span>
        <%= link_to "Logout", session_path, method: :delete, 
            data: { turbo_method: :delete },
            class: "px-3 py-2 rounded text-gray-600 hover:bg-gray-100 transition duration-150" %>
      </div>
    <% else %>
      <%= link_to "Login", new_session_path, class: "px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700 transition duration-150" %>
    <% end %>
  </div>
</nav>
